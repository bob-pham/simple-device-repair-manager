package ui.viewdevices;

public class LaptopTable extends DeviceTable {
    public LaptopTable() {
        header = new String[] {"Service Num", "Repaired By", "Repair Progress", "Brand", "Power",
                "Storage (Hard drive + Solid State)", "Battery", "Screen Condition", "Hard Drive Caddy", "Ram", "CPU",
                "GPU", "OS", "Additional Notes"};
    }
}
